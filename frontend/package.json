{
  "name": "snap-eats-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "npm run build:react && npm run build:static",
    "build:react": "vite build",
    "build:static": "node scripts/prepare-static-site.mjs",
    "preview": "vite preview --host 127.0.0.1 --outDir dist"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.2",
    "vite": "^8.0.16"
  }
}
